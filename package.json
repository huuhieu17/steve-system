{
  "name": "system-app",
  "private": true,
  "scripts": {
    "build": "npm run build:computer-control && npm run build:shell",
    "build:computer-control": "cd computer-control && npm run build",
    "build:shell": "cd shell-app && npm run build",
    "start": "cd shell-app && yarn dev"
  },
  "dependencies": {
    "@module-federation/bridge-react": "^2.0.0",
    "@module-federation/bridge-vue3": "^2.0.0",
    "@module-federation/runtime": "^2.0.0"
  }
}
